[project]
name = "artsi"
version = "0.1.0"
description = "A Python module for drawing in the terminal with ANSI escape codes."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pynput>=1.8.2",
]
